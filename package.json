{
    "name": "archalive-root",
    "private": true,
    "scripts": {
        "build": "cd simulation && wasm-pack build --target web --out-dir pkg && cd .. && npm install --prefix client && npm run build --prefix client",
        "vercel-build": "bash vercel-build.sh"
    },
    "devDependencies": {
        "wasm-pack": "^0.13.1"
    }
}